"""permea-eval/1.0 contract (placeholder).

The evaluation contract (warning-code families, ``identity_definition`` requirement,
``headline_condition`` constant, ``resample_unit`` default, ``additionalProperties: false``)
is not yet formalised here; it currently lives only as inline references in the engine,
bootstrap, cluster, and dataset-loading modules. This package is the intended home for the
written schema. Left empty deliberately -- see the port design notes.
"""
